import ModalComponent from '../ModalComponent';

const SigninComponent: React.FC = () => {
  return (
    <ModalComponent
      title='Signin to your account'
      body="or create one if you haven't already."
    />
  );
};

export default SigninComponent;
